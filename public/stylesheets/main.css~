@charset "utf-8";
/* Reset */
*{margin:0; padding:0}
:focus{outline:0 none}
h1, h2, h3, h4, h5, h6, p, pre, blockquote, ul, ol, dl, table, address{margin-bottom:14px}
dd{margin-bottom:5px}
img{border:0}
ul, ol, dd{padding-left:24px}
a{text-decoration:underline}
a:hover{text-decoration:none}
label{cursor:pointer}
table{width:100%; border:0 none; border-collapse:separate; border-spacing:0}
th{text-align:left}
th, td{padding:0}
/* Typography */
body{font-family:Helvetica,Arial,sans-serif; line-height:16px; background:#fff; color:#000}
h1{font-size:48px; line-height:0.5em}
h2{font-size:36px; line-height:0.666em}
h3{font-size:32px; line-height:0.75em}
h4{font-size:24px; line-height:1em}
h5{font-size:18px; line-height:1.333em}
h6{font-size:16px; line-height:1.5em}
dt{font-weight:bold}
p, table, ol, ul, dl, pre, blockquote, address{font-size:16px}
/* Clearfix */
.clearfix:after{content:"."; display:block; clear:both; visibility:hidden; line-height:0; height:0}
.clearfix{display:inline-block}
html[xmlns] .clearfix{display:block}
* html .clearfix{height:1%}
/* Phark Image Replacement */
.imgheader, .imgbutton{text-indent:-9999px; overflow:hidden; background-repeat:no-repeat}
.imgbutton{display:block}


/* body styles */
body {
  background-color: #338080;
  color: #cccc99;
  width: 640px;
  margin: 0 auto;
}
body p {
  font-size: 16px;
  line-height: 1.5em;
}
#container {
  background-color: #cccc99;
  color: #191419;
}
#container article {
  padding: 48px;
}
#container h1, h2, h3, h4, h5, h6 {
  text-shadow: 0 -1px 0 #fff;
}
a:link {
  color: #cc334d;
  text-decoration: none;
}
a:hover {
  color: #fff; 
  background-color:#cccc99; 
  text-decoration: underline;
}
a:active {
  color: #cc334d;
  background-color:#cccc99; 
  text-decoration: none;
}
a:visited {
  color: #cc334d;
  background-color:#cccc99; 
  text-decoration: none;
}
hgroup a {
  color: #cccc99;
  background-color: #338080;
  font-size: 150px;
  line-height: 145px;
  text-decoration: none;
  text-shadow: 0 -1px 0 #fff;
}
hgroup a:link {
  color: #cccc99;
  background-color: #338080;
}
hgroup a:hover {
  color: #fff;
  background-color: #000;
  text-decoration: none;
}
